# 巨量千川直播监测接入说明

## 当前实现

`qianchuan_monitor_app.py` 是一个只读监测软件，调用巨量引擎官方 Marketing API 的“获取今日直播数据”接口：

```text
GET https://api.oceanengine.com/open_api/v1.0/qianchuan/report/live/get/
```

当前监测指标包括广告消耗、累计成交金额、整体 ROI、广告 ROI、观看人次、广告成单数、点击率、转化数、粉丝、评论和商品点击。软件按设置间隔轮询，最低间隔限制为 30 秒，并支持阈值预警和 CSV 导出。

本软件没有创建计划、修改预算、修改出价等写接口。

## 先体验演示模式

双击 `start_qianchuan_monitor.bat`，保持“演示模式”勾选，点击“开始监测”。演示模式不会访问互联网或任何千川账户。

## 连接真实千川数据

需要先在[巨量引擎商业开放平台](https://open.oceanengine.com/)完成以下准备：

1. 注册并认证开发者。
2. 新建“巨量千川”应用。自用场景通常选择“自研投放系统”。
3. 为应用申请千川“数据报表/直播间报表”相关权限。
4. 使用官方 OAuth2.0 流程，让目标账户授权应用并获取 Access Token。
5. 获取已授权的千川广告主 ID，以及需要筛选时使用的抖音号 ID。

在软件中取消“演示模式”，填写：

- `Access Token`：OAuth 授权得到的访问凭证。
- `广告主 ID`：千川广告账户 ID，必填。
- `抖音号 ID`：可选；填写后只查询指定授权抖音号。

点击“立即刷新”测试连接，成功后再点击“开始监测”。

## 凭证安全

- Access Token 只保存在软件当前进程的内存中，关闭软件即清除。
- 软件不会把 Token 写入 CSV、日志或配置文件。
- 不要把 Token、APP Secret 发到聊天记录、截图或代码仓库中。
- 正式部署建议增加 OAuth 回调服务和加密凭证存储，而不是长期手工粘贴 Token。

## 数据时效说明

软件展示的是官方报表接口返回的“今日累计数据”。它不是对千川网页进行抓取，因此稳定性和合规性更好，但数据刷新速度受账户权限、接口频控和官方报表延迟影响。最低 30 秒的轮询限制是本软件的保护措施，不代表官方承诺 30 秒内更新。

## 生成 EXE

双击 `build_qianchuan_monitor.bat`，完成后得到：

```text
dist\QianchuanLiveMonitor.exe
```
